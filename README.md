step 1 setup
    + create file .env in vscode COS30018-BUSINESS-AGENT
    + copy this and paste into .env file
      GEMINI_API_KEY= {your api key}
      YELP_API_KEY= {your api key}
    + create api key and change it into .env file

